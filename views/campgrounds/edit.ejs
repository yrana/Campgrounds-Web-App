<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit campground</title>
</head>

<body>

    <h1>Editing <%= campSelected.title %>
    </h1>

    <form action="/campgrounds/<%=campSelected.id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="campground[title]" value="<%=campSelected.title %> ">
        </div>

        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="campground[location]" value="<%=campSelected.location %> ">
        </div>

        <button>Update campground</button>
    </form>

</body>

</html>